const categorySearchFields = ["name", "description", "slug"];
const categoryFiltersFields = ["searchTerm", "name", "slug", "parent_cat_id"];

export const CategoryConstant = {
  categorySearchFields,
  categoryFiltersFields,
};
